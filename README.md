# MediBot
AI Medical Chatbot (MediBot): A Retrieval-Augmented Generation (RAG) project featuring LangChain, FAISS vector store, and a Streamlit UI.
How to run
uv run -r requirements.txt
add API key in env file i cant upload becauuse its a secret key
uv run streamlit run medibot.py
